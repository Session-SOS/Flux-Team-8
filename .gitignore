# ============================================================
# Flux — Git Ignore
# Prevents build artifacts, secrets, and OS junk from being
# committed to version control.
# ============================================================

# ---------- Node / Frontend ----------
node_modules/
dist/
.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---------- Python / Backend ----------
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
*.egg-info/
*.egg

# ---------- Environment files ----------
# NEVER commit real secrets — only .env.example is tracked
.env
.env.local
.env.*.local

# ---------- IDE ----------
.idea/
# Allow shared VS Code settings and extension recommendations
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json

# ---------- OS ----------
.DS_Store
Thumbs.db
Desktop.ini
*.code-workspace

# ---------- Build artifacts ----------
dist/
build/

# ---------- Test reports & coverage ----------
TEST_REPORT.md
test-results/
htmlcov/
.coverage
coverage.xml

# ---------- Logs ----------
*.log

# ---------- Supabase ----------
# Track config.toml but ignore local runtime data
supabase/.branches
supabase/.temp

# ---------- Docker ----------
docker-compose.override.yml

# ---------- PWA dev artifacts ----------
dev-dist/
